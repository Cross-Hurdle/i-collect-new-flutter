import 'package:icollekt/Exports/Exports.dart';

class UploadTitle extends StatelessWidget {
  const UploadTitle({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: topappbar(),
    );
  }
  AppBar topappbar(){
    return AppBar(

    );
  }
}